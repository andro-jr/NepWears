"use strict";

// SLick slider settings
$(document).ready(function () {
  $(".hero-slide").slick({
    dots: true,
    arrows: true,
    autoplay: true,
    autoplaySpeed: 1000,
    dots: true,
    infinite: true,
    speed: 4000,
  });
});
